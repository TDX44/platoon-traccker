services:
  app:
    build: .
    ports:
      - "5000:5000"
    environment:
      SECRET_KEY: ${SECRET_KEY:-change-me-in-production}
      DATA_DIR: /data
    volumes:
      - platoon_data:/data
    restart: unless-stopped

volumes:
  platoon_data:
